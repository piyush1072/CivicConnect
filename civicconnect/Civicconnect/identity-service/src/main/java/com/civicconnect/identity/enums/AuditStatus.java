package com.civicconnect.identity.enums;

public enum AuditStatus {
    OPEN,       // Audit initiated, findings pending
    IN_REVIEW,  // Findings being reviewed
    CLOSED      // Audit completed
}
